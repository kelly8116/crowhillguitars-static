---
label: Image Gallery
hide_body: true
fields:
- name: image_select
  type: field_group_list
  fields:
  - name: image
    type: file
    config:
      maxSize: 64
    label: Image
  - name: tags
    type: tag_list
    default: []
    label: Tags
  config:
    min: 
    max: 
    labelField: 
  label: Image Select
